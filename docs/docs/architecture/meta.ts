import { defineMeta } from "blume";

export default defineMeta({
  title: "Architecture",
  icon: "layout",
  order: 2,
  pages: ["setup", "architecture", "architecture-review", "security-model", "policy-config", "limitations"],
});
